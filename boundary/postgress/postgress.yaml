global:
  storageClass: longhorn
architecture: "replication"
primary:
  persistence:
    size: 200Mi

replica:
  replicaCount: 2
  persistence:
    size: 200Mi

auth:
  existingSecret: postpwd


replication:
  enabled: true
